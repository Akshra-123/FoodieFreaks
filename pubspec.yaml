name: new_flutter
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  camera: 0.11.0+2
  gal: ^2.3.1
  salomon_bottom_bar: ^3.3.2
  font_awesome_flutter: ^10.8.0  # Updated to the latest version
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  

flutter:
  uses-material-design: true

  # Adding assets to the project
  assets:
    - assets/splash.png
    - assets/homeimg.png
    - assets/lays.png
    - assets/fruit_salad.png
    

